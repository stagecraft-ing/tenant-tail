# Business Requirements Document

## Scope

The tenant application re-checks its own governance paperwork.

### BR-001: Verify governance certificates

The system verifies the governance certificate before trusting the build.

### BR-002: Re-check claim provenance

Every claim minted during the build carries a provenance mode and is
re-checked against the extraction corpus.
